[SAVE BUTTON]save list back to a json in the format specified in 
	saveButton action listener will be saving to JSON

set up Adapter, read in exotic file (two more lines under the reading in the controller)

[comboBox] SORTING( then put options in the dropdown menu )

to dane: right now i have the ID field as a String, could change or could not, idrc

should probably flash messages when delete, adopt, add is completed

change so that only pet has adoptable